# 記事管理ルール

公開URLは固定し、「1人のAV女優＝1記事」で管理します。店舗ごとに記事を分けません。

## 新規記事の作成前

1. 女優名、読み、店舗掲載名、登録済みの別名をすべて指定して重複検索します。

   ```sh
   node check-article-duplicate.mjs "女優名" "読み" "店舗掲載名" "別名"
   ```

2. 既存記事が表示された場合は、新規ファイルを作らず、その記事へ店舗カードを追加します。
3. 一致しない場合だけ `article-template.html` から新規記事を作り、公開前に `article-registry.json` へ1件登録します。
4. 引数なしでも台帳全体を検査できます。

   ```sh
   node check-article-duplicate.mjs
   ```

## 既存記事の更新

- 新しい在籍店舗は「現在確認できる在籍店舗」に別カードで追加します。
- 各カードへ店舗名、都道府県、エリア、店舗掲載名、公式URL、確認方法、最終確認日を記録します。
- 記事全体の最終確認日を更新し、更新履歴へ追記します。
- `article-registry.json` の `storeNames` と、判明した場合は `aliases` も更新します。
- トップページと記事一覧では女優を1件だけ表示し、複数店舗の場合は「在籍確認店舗：N店舗」とします。

## 掲載終了を確認した店舗

店舗カードを削除せず、「過去に確認した在籍店舗」へ移動します。店舗名、掲載名、最終確認日と「現在は公式サイトで掲載を確認できません」を残します。確認できないURLを新規に推測して追加しません。

## 公開前確認

- 台帳の重複検査が成功すること
- 同じ女優が `index.html` と `actresses.html` に複数ないこと
- 既存記事のファイル名、canonical、内部リンクを変更していないこと
- 現在の店舗と過去の店舗が別セクションで表示されること
- 公式ボタンに `target="_blank"` と `rel="noopener noreferrer"` があること
